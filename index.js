// Importing all the modules
const asyncTools = require('./asyncTools');
const dataValidation = require('./dataValidation');
const customModules = require('./customModules');
const errorHandling = require('./errorHandling');
const thirdPartyIntegration = require('./thirdPartyIntegration');
const resourceManagement = require('./resourceManagement');

// Exporting all the modules
module.exports = {
  asyncTools,
  dataValidation,
  customModules,
  errorHandling,
  thirdPartyIntegration,
  resourceManagement
};
